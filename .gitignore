# ----------------------------------------------------------------------
# 1. FICHIERS GENERAUX DU SYSTEME (OS)
# ----------------------------------------------------------------------
.DS_Store
Thumbs.db

# ----------------------------------------------------------------------
# 2. DEPENDANCES ET BUILD DU FRONTEND (Vue.js)
# ----------------------------------------------------------------------
# Dépendances (dans /front)
/front/node_modules

# Dossier de build (compilation du code Vue.js)
/front/dist
/front/build
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ----------------------------------------------------------------------
# 3. DEPENDANCES ET FICHIERS GENERES DU BACKEND (PHP)
# ----------------------------------------------------------------------
# Dépendances gérées par Composer (dans /back)
/back/vendor/
#/back/composer.lock # Décommentez si vous voulez l'ignorer

# Fichiers de configuration sensibles ou logs
/back/.env
/back/.env.local

# Dossiers de cache, logs ou sessions (ajustez selon votre framework)
/back/storage/
/back/var/cache/
/back/var/log/
/back/public/uploads/